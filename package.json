{
  "name": "workforce-hermes-root",
  "private": true,
  "scripts": {
    "build": "cd Transition && npm install && npm run build && cd .. && mkdir -p dist && cp -r Transition/dist/* dist/",
    "install": "cd Transition && npm install"
  }
}
